package constants

// Configuration name constants — used as the "name" field in the configurations collection.
const (
	ConfigNameAppLanguage = "app_language"
)

// Platform constants — used as the "platform" field in the configurations collection.
const (
	PlatformIOS     = "ios"
	PlatformAndroid = "android"
	PlatformWeb     = "web"
)
