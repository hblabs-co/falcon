// Copy this file to Config.swift and fill in your values.
// Config.swift is gitignored and must never be committed.
enum Config {
    static let apiURL = "http://your-api-server:8080"
    static let userID = "your-nanoid-user-id"
    static let imageHost = "http://your-minio-host:9000"
    static let realtimeURL = "ws://your-realtime-server:8090/ws"
    static let realtimeSecret = "shared-secret-matching-server-REALTIME_SHARED_SECRET"
}
